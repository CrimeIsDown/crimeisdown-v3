<figure>
  <figcaption>{{yield}}</figcaption>
  <audio {{did-insert (fn this.initializeMediaPlayer @stream @autoplay)}} id="stream-player-{{@stream.slug}}" class="stream-player stream-player-wide" preload="none">
    Your browser does not support the <code>audio</code> element.
  </audio>
</figure>